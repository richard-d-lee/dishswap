// Using custom authentication

// Stub exports to maintain compatibility with existing imports
export const sdk = {
  // Legacy SDK - no longer used
};

export function initializeSDK() {
  // No-op: Using custom authentication
}
