// Manus OAuth SDK removed - using custom authentication

// Stub exports to maintain compatibility with existing imports
export const sdk = {
  // Legacy Manus SDK - no longer used
};

export function initializeSDK() {
  // No-op: Manus OAuth has been replaced with custom authentication
}
